<Window x:Class="Bibliothicc.SettingsWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bibliothicc"
        mc:Ignorable="d"
        Title="Settings" Height="220" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgDeepBrush}">
    <Grid>
        <Border Background="{DynamicResource BgSurfaceBrush}"
                CornerRadius="12" Margin="16"
                BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1">
            <StackPanel Margin="24,20">
                <TextBlock Text="Settings" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="0,0,0,24"/>

                <!-- Darkmode toggle row -->
                <Border Background="{DynamicResource BgElevatedBrush}"
                        CornerRadius="8" Padding="14,12" Margin="0,0,0,12">
                    <DockPanel>
                        <Button x:Name="ButtonDarkmode" DockPanel.Dock="Right"
                                Style="{DynamicResource IconButton}"
                                Background="Transparent" BorderThickness="0"
                                Width="50" Height="26"
                                Click="Button_Click">
                            <Image Source="/Bilder/button_aus.png" Width="44" Height="22"/>
                        </Button>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Dark Mode" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="Toggle dark/light theme" FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
